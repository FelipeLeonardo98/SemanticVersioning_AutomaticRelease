on:
  release:
    types:
      - created

jobs:
  update-changelog:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Update CHANGELOG.md
        uses: requarks/changelog-action@v2.1.0
        with:
          changelog-file: CHANGELOG.md